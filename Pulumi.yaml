name: image-discerner
runtime: python
description: A serverless image analysis service using AWS and GCP
config:
  aws:region:
    description: AWS region for deployment
    default: us-west-2
  gcp:project:
    description: GCP project ID for Vision API
  gcp:region:
    description: GCP region
    default: us-central1